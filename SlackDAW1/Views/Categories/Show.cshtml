@model SlackDAW1.Models.Category

@{
    ViewBag.Title = "Category Details: " + Model.CategoryName;
}

<h2>@Model.CategoryName</h2>

<h3>Channels in this Category:</h3>
<ul>
    if (ViewBag.Channels != null)
	{
        @foreach (var channel in ViewBag.Channels)
        {
            <li>@channel.ChannelName</li>
        }
    } else {
		<li>No channels in this category</li>
	}
</ul>

<!-- Add additional category details here if needed -->
